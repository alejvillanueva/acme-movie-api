const { expect } = require('chai');

describe('Basic Test', () => {
  it('Passes A Test', () => {
    expect('hello').to.equal('hello');
  });
  it('Passes Another Test', () => {
    expect('hello').to.equal('hello');
  });
});
